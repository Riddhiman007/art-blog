import React, { useState } from "react";
import Avatar, { Props } from "react-avatar-edit";

export default function AvatarUploader(props: Props) {
  // console.log(props);

  return <Avatar {...props} />;
}
